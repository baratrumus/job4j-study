package multithread.nonblock;

public class Base {
        int id;
        int version;

        public Base(int id, int version) {
            this.id = id;
            this.version = version;
        }
}
